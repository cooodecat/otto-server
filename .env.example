# Otto Server 환경 설정 템플릿
#
# 사용 방법:
# 1. 개발: 이 파일을 .env.development로 복사 후 실제 값 입력
# 2. 프로덕션: 이 파일을 .env로 복사 후 실제 값 입력
#
# ⚠️  중요: 실제 값이 들어간 .env.dev나 .env는 절대 Git에 커밋하지 마세요!

# =============================================================================
# 기본 설정
# =============================================================================

# 서버 포트
PORT=4000  # 개발: 4000, 프로덕션: 3000 권장

# 실행 환경
NODE_ENV=development  # development 또는 production

# =============================================================================
# Supabase Cloud 설정
# =============================================================================

# Supabase 프로젝트 URL
# Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (Public)
# Supabase Dashboard > Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase JWT Secret (서버 전용, 절대 노출 금지!)
# Supabase Dashboard > Settings > API > JWT Settings > JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Supabase 프로젝트 ID
# URL에서 추출: https://[PROJECT_ID].supabase.co
SUPABASE_PROJECT_ID=your-project-id

# =============================================================================
# GitHub OAuth 설정
# =============================================================================

# GitHub OAuth App Client ID
# GitHub > Settings > Developer settings > OAuth Apps > New OAuth App
OTTO_GITHUB_CLIENT_ID=your-github-client-id

# GitHub OAuth App Secret (절대 노출 금지!)
OTTO_GITHUB_OAUTH_SECRET=your-github-oauth-secret

# OAuth Callback URL
# 형식: https://your-project-id.supabase.co/auth/v1/callback
OTTO_GITHUB_CALLBACK_URL=https://your-project-id.supabase.co/auth/v1/callback

# =============================================================================
# CORS 설정
# =============================================================================

# 프론트엔드 URL
FRONTEND_URL=http://localhost:3000

# 허용된 Origin 목록 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# =============================================================================
# 향후 기능 (선택사항)
# =============================================================================

# GitHub App (Webhook, GitHub Actions 통합용)
# OTTO_GITHUB_APP_ID=
# OTTO_GITHUB_APP_NAME=
# OTTO_GITHUB_APP_PRIVATE_KEY=
# OTTO_GITHUB_WEBHOOK_SECRET=

# =============================================================================
# AWS CloudWatch Logs API 설정 (필수)
# =============================================================================

# AWS 리전
AWS_REGION=ap-northeast-2

# AWS Access Key ID (CloudWatch Logs 및 CodeBuild 권한 필요)
# 영구 자격 증명만 사용 (AKIA로 시작하는 키)
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key (절대 노출 금지!)
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# 참고: Session Token은 사용하지 않음 (영구 자격 증명 사용)

# AWS CodeBuild (CI/CD)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
CODEBUILD_PROJECT_NAME=your-codebuild-project-name
AWS_CODEBUILD_SERVICE_ROLE=
CODEBUILD_ARTIFACTS_BUCKET=
CODEBUILD_PROJECT_NAME=your-project-name
CODEBUILD_TIMEOUT=60  # 분 단위
CODEBUILD_CONCURRENT_BUILDS=5
